// 06 - Crea un componente Loading. Questo apparirà nella pagina quando la richiesta è in fase di risoluzione, ma non ancora risolta.

import { Spinner } from 'react-bootstrap'

const Loading = () => <Spinner animation="border" variant="success" />

export default Loading
